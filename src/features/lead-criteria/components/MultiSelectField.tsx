/**
 * Promoted to `@/components/ui/MultiSelectField` once Reports' "Group by"
 * field needed the same closed-list multi-select this form built first.
 * Re-exported from its old path in case anything still imports from here;
 * new call sites should import from `@/components/ui` directly.
 */
export { MultiSelectField } from '@/components/ui/MultiSelectField';
